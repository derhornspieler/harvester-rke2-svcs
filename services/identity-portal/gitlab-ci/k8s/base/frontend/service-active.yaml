apiVersion: v1
kind: Service
metadata:
  name: identity-portal-frontend-active
  namespace: identity-portal
spec:
  type: ClusterIP
  selector:
    app: identity-portal
    component: frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

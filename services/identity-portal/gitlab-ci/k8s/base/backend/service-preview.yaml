apiVersion: v1
kind: Service
metadata:
  name: identity-portal-backend-preview
  namespace: identity-portal
spec:
  type: ClusterIP
  selector:
    app: identity-portal
    component: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

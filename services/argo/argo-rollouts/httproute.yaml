apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argo-rollouts
  namespace: argo-rollouts
spec:
  parentRefs:
    - name: argo-rollouts
      namespace: argo-rollouts
      sectionName: https
  hostnames:
    - "rollouts.CHANGEME_DOMAIN"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: basic-auth-rollouts
      backendRefs:
        - name: argo-rollouts-dashboard
          port: 3100

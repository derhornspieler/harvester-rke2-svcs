apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: alertmanager
  namespace: monitoring
  annotations:
    cert-manager.io/cluster-issuer: vault-issuer
spec:
  gatewayClassName: traefik
  listeners:
    - name: https
      protocol: HTTPS
      port: 8443
      hostname: alertmanager.CHANGEME_DOMAIN
      tls:
        mode: Terminate
        certificateRefs:
          - name: alertmanager-CHANGEME_DOMAIN_DASHED-tls
      allowedRoutes:
        namespaces:
          from: Same

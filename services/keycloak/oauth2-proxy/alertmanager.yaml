apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth2-proxy-alertmanager
  namespace: monitoring
  labels:
    app.kubernetes.io/name: oauth2-proxy
    app.kubernetes.io/instance: alertmanager
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: oauth2-proxy
      app.kubernetes.io/instance: alertmanager
  template:
    metadata:
      labels:
        app.kubernetes.io/name: oauth2-proxy
        app.kubernetes.io/instance: alertmanager
    spec:
      nodeSelector:
        workload-type: general
      containers:
        - name: oauth2-proxy
          image: quay.io/oauth2-proxy/oauth2-proxy:v7.8.1
          args:
            - --provider=keycloak-oidc
            - --client-id=alertmanager-oidc
            - --oidc-issuer-url=https://keycloak.CHANGEME_DOMAIN/realms/CHANGEME_KC_REALM
            - --oidc-groups-claim=groups
            - --allowed-group=platform-admins
            - --allowed-group=infra-engineers
            - --redirect-url=https://alertmanager.CHANGEME_DOMAIN/oauth2/callback
            - --upstream=static://202
            - --skip-provider-button=true
            - --cookie-secure=true
            - --cookie-samesite=lax
            - --cookie-name=_oauth2_proxy_alertmanager
            - --http-address=0.0.0.0:4180
            - --metrics-address=0.0.0.0:44180
            - --email-domain=*
            - --provider-ca-file=/etc/ssl/certs/vault-root-ca.pem
            - --skip-jwt-bearer-tokens=true
            - --code-challenge-method=S256
            - --reverse-proxy=true
            - --prompt=login
            - --session-store-type=cookie
            - --whitelist-domain=.CHANGEME_DOMAIN
          env:
            - name: OAUTH2_PROXY_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth2-proxy-alertmanager
                  key: client-secret
            - name: OAUTH2_PROXY_COOKIE_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth2-proxy-alertmanager
                  key: cookie-secret
          ports:
            - containerPort: 4180
              name: http
              protocol: TCP
            - containerPort: 44180
              name: metrics
              protocol: TCP
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: vault-root-ca
              mountPath: /etc/ssl/certs/vault-root-ca.pem
              subPath: ca.crt
              readOnly: true
      volumes:
        - name: vault-root-ca
          configMap:
            name: vault-root-ca
---
apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy-alertmanager
  namespace: monitoring
  labels:
    app.kubernetes.io/name: oauth2-proxy
    app.kubernetes.io/instance: alertmanager
spec:
  type: ClusterIP
  ports:
    - port: 4180
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: oauth2-proxy
    app.kubernetes.io/instance: alertmanager

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-pg-credentials
  namespace: database
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: grafana-pg-credentials
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
  data:
    - secretKey: username
      remoteRef:
        key: services/database/grafana-pg
        property: username
    - secretKey: password
      remoteRef:
        key: services/database/grafana-pg
        property: password

defaultNamespace: harbor
dependsOn:
  - selector:
      matchLabels:
        fleet.cattle.io/bundle-name: minio
  - selector:
      matchLabels:
        fleet.cattle.io/bundle-name: identity-keycloak-config
  - selector:
      matchLabels:
        fleet.cattle.io/bundle-name: pki-vault-bootstrap-store
targets:
  - clusterName: ${FLEET_TARGET_CLUSTER}

defaultNamespace: vault
helm:
  releaseName: vault
  chart: ${OCI_HELM_PREFIX}/vault
  version: "${CHART_VER_VAULT}"
  valuesFiles:
    - values.yaml
targets:
  - clusterName: ${FLEET_TARGET_CLUSTER}

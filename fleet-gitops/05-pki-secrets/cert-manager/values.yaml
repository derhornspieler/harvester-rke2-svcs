crds:
  enabled: true

# Gateway API shim for Certificate resources
extraArgs:
  - --enable-gateway-api

# Prometheus ServiceMonitor
# Disabled: CRDs from kube-prometheus-stack may not be available yet
# Re-enable once monitoring is deployed
prometheus:
  enabled: false
  servicemonitor:
    enabled: false

defaultNamespace: cert-manager
helm:
  releaseName: cert-manager
  chart: ${OCI_HELM_PREFIX}/cert-manager
  version: "${CHART_VER_CERT_MANAGER}"
  valuesFiles:
    - values.yaml
targets:
  - clusterName: ${FLEET_TARGET_CLUSTER}

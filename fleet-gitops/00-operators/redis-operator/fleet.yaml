defaultNamespace: redis-operator
helm:
  releaseName: redis-operator
  chart: oci://harbor.aegisgroup.ch/helm/redis-operator
  version: "0.23.0"
  valuesFiles:
    - values.yaml
targets:
  - clusterName: rke2-prod

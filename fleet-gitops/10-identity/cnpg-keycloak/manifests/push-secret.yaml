# Password generator + PushSecret for Keycloak PostgreSQL credentials
# Generates password and combines with static username "keycloak",
# then pushes to Vault for the ExternalSecret to pull from
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: keycloak-pg-password-gen
  namespace: database
spec:
  length: 32
  digits: 5
  symbols: 0
  noUpper: false
  allowRepeat: true
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloak-pg-generated-credentials
  namespace: database
spec:
  refreshInterval: "0"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: keycloak-pg-generated-credentials
    creationPolicy: Owner
    template:
      mergePolicy: Merge
      data:
        username: "keycloak"
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: keycloak-pg-password-gen
      rewrite:
        - regexp:
            source: "password"
            target: "password"
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: keycloak-pg-credentials-push
  namespace: database
spec:
  updatePolicy: IfNotExists
  secretStoreRefs:
    - name: vault-writer
      kind: SecretStore
  selector:
    secret:
      name: keycloak-pg-generated-credentials
  data:
    - match:
        secretKey: username
        remoteRef:
          remoteKey: services/database/keycloak-pg
          property: username
    - match:
        secretKey: password
        remoteRef:
          remoteKey: services/database/keycloak-pg
          property: password

defaultNamespace: external-dns
helm:
  releaseName: external-dns
  chart: oci://harbor.example.com/helm/external-dns
  version: "1.16.1"
  valuesFiles:
    - values.yaml
dependsOn:
  - selector:
      matchLabels:
        fleet.cattle.io/bundle-name: pki-external-secrets
targets:
  - clusterName: rke2-prod

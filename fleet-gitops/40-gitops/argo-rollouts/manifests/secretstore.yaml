apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets
  namespace: argo-rollouts
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vault-backend
  namespace: argo-rollouts
spec:
  provider:
    vault:
      server: "http://vault.vault.svc.cluster.local:8200"
      path: "kv"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "eso-argo-rollouts"
          serviceAccountRef:
            name: "external-secrets"

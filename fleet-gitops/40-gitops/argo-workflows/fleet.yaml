defaultNamespace: argo-workflows
helm:
  releaseName: argo-workflows
  chart: ${OCI_HELM_PREFIX}/argo-workflows
  version: "${CHART_VER_ARGO_WORKFLOWS}"
  valuesFiles:
    - values.yaml
targets:
  - clusterName: ${FLEET_TARGET_CLUSTER}

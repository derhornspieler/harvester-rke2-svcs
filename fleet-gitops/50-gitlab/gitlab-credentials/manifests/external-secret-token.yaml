apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gitlab-praefect-secret
  namespace: gitlab
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: gitlab-praefect-secret
    creationPolicy: Owner
  data:
    - secretKey: token
      remoteRef:
        key: services/gitlab/praefect-secret
        property: token

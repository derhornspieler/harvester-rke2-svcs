# Root CA certificate for GitLab to trust Vault/Keycloak TLS
# GitLab certificates.customCAs expects a Secret with .crt key
apiVersion: v1
kind: Secret
metadata:
  name: gitlab-root-ca
  namespace: gitlab
type: Opaque
stringData:
  root-ca.crt: |
${ROOT_CA_PEM_INDENT4}

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gitlab-license
  namespace: gitlab
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: gitlab-license
    creationPolicy: Owner
  data:
    - secretKey: license
      remoteRef:
        key: services/gitlab
        property: license
    - secretKey: license-encryption-key
      remoteRef:
        key: services/gitlab
        property: license-encryption-key

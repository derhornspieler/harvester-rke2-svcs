defaultNamespace: gitlab
dependsOn:
  - selector:
      matchLabels:
        fleet.cattle.io/bundle-name: gitlab-init
  - selector:
      matchLabels:
        fleet.cattle.io/bundle-name: operators-redis
targets:
  - clusterName: ${FLEET_TARGET_CLUSTER}
